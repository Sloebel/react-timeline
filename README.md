# react-timeline
React Timeline is a virtualized, zoomable timeline grid that shows time-bounded items in rows (with clustering, tooltips, selection, and a current-time indicator), aimed at scheduling and planning UIs.
